
<% if @current_location %>
<div class="container has-location">
  <p><%= @current_location.longitude %></p>
  <p><%= @current_location.latitude %></p>
  <%= form_for( @location, url: api_v1_locations_path, remote: true ) do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.hidden_field :longitude %>
    <%= f.hidden_field :latitude %>
    <%= f.submit "Update Location", class: "btn btn-default btn-sand js-submit" %>
  <% end %>
</div>  
<% else %>
<div class="container no-location">
  <%= form_for( @location, url: api_v1_locations_path, remote: true ) do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.hidden_field :longitude %>
    <%= f.hidden_field :latitude %>
    <%= f.submit class: "js-submit" %>
  <% end %>
</div>
<% end %>

<p><%= current_user.email %></p>